export class User {
    constructor(id, firstName, lastName, email, Dogs) {
      this.id = id;
      this.first_name = firstName;
      this.last_name = lastName;
      this.email = email;
      this.Dogs = Dogs;
  }
}