export class User {
    constructor(id, firstName, lastName, email, Dogs) {
      this.id = id;
      this.firstName = firstName;
      this.lastName = lastName;
      this.email = email;
      this.Dogs = Dogs;
  }
}